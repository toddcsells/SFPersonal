public with sharing class MessageSuccessHandler implements IMessageHandler {
    public string LoadMessage(string MessageType){
    	return '';
    }
    
    public string ShowMessage(){
    	string message = 'Success';
    	return message;
    }
}